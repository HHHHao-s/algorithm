#include "../s.h"


class Solution {
public:
    int subsequencePairCount(vector<int>& nums) {
        
        vector<int> each(201);
        for(int num : nums){
            each[num] ++;
        }
        

    }
};
